# Dependencies (staged at build time from the installed extension; never commit)
node_modules/

# Build artifacts (VSIX + install helpers generated by the fork-sync skill)
*.vsix
install-test-vsix.cmd

# TIA Portal extension runtime state (regenerated)
.tia/
.tia-cache/
TiaExport/
